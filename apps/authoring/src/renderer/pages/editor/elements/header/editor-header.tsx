import React from 'react';
import { Logo, Toolbar } from '../../../../components';

export const Header = () => {
  return (
    <Toolbar>
      <Logo />
    </Toolbar>
  );
};

export default {
  Header,
};
