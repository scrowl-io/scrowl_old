/**
 * This interface should be updated once
 * define the actual project structure.
 */
export interface Project {
  id: string;
  createdAt: string;
  modifiedAt: string;
  name: string;
  description: string;
  theme: string;
}
